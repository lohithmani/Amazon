# Setup

## Frontend

```bash
cd frontend
npm install
npm run build
npm run dev
```

## Notes

- The build is custom and uses esbuild directly.
- The app currently runs on mocked data.
- Backend service wiring can be added behind the existing service layer.
